{% if admin.hasRoute('test') %}
    <a
            href="{{ admin.generateObjectUrl('test', object, actions.link_parameters|default([])) }}"
            class="btn btn-sm btn-default"
            title="Test"
    >
        <i class="fa fa-check" aria-hidden="true"></i>
        Test
    </a>
{% endif %}
